---
// src/components/TimelineStep.astro
const { title, date } = Astro.props;
---

<div class="timeline-step">
  <div class="timeline-content">
    {date && <span class="step-date">{date}</span>}
    <h4>{title}</h4>
    <slot /> 
  </div>
</div>